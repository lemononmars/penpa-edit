{
  "id": "fullrank",
  "name": "Full Rank",
  "rules": {
    "6x6": "Place a digit from 1 to 6 into each empty cell in the grid so that each digit appears exactly once in each row, column and 2x3 outlined box. Each row and column forms two n-digit numbers, where n is the grid size, read in opposite directions. Each number outside the grid indicates the rank of the corresponding n-digit number, starting from the smallest.",
    "9x9": "Place a digit from 1 to 9 into each empty cell in the grid so that each digit appears exactly once in each row, column and 3x3 outlined box. Each row and column forms two n-digit numbers, where n is the grid size, read in opposite directions. Each number outside the grid indicates the rank of the corresponding n-digit number, starting from the smallest."
  },
  "tags": ["outside"]
}
