Ah wait! If the grid is, say, a 6x6 visual grid and requires digits 1..9, the user might select grid size 6x6.
`puzzleSize(puzzle)` would return 6.
But Tight Fit requires digits 1..9 in a 6x6 grid!
So we need to override the `SIZE` sent to CSP, maybe to 9.
But wait, what if the grid is 5x5?
Actually, what sizes does Tight Fit typically use? 6x6 with digits 1..9.
If we set `SIZE = 9`, the `board` matrix in CSP is 9x9.
Wait, if it's a 6x6 board where some cells have 2 digits, we have a total of exactly 6 columns, 6 rows. But there are 6*6=36 visual cells. If each row has 9 digits, then each row has exactly 3 split cells (3*2 + 3*1 = 9 digits).
How do we model this in ALX (Dancing Links)?
Currently, `docs/js/sudoku_csp.js` uses an exact cover internally if we don't modify it too much. No, `docs/js/sudoku_csp.js` doesn't use Dancing Links. It uses backtracking search with bitmasks.
Let's verify how `docs/js/sudoku_csp.js` does search.
